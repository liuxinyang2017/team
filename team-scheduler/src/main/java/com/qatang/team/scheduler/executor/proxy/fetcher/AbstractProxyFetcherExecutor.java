package com.qatang.team.scheduler.executor.proxy.fetcher;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * @author qatang
 */
public abstract class AbstractProxyFetcherExecutor implements IProxyFetcherExecutor {
    protected final Logger logger = LoggerFactory.getLogger(this.getClass());
}
